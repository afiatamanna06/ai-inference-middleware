# AI Inference Middleware for Multiple Models

This project will implement middleware to run multiple AI models concurrently using:
- CPU parallelism (OpenMP / multithreading)
- GPU acceleration (CUDA / PyTorch)
- Distributed nodes (MPI / gRPC)
- TypeScript CLI and dashboard for user interaction

